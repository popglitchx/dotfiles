# Window settings
# window_padding_width 10
# window_border_width 0
# window_title_with_platform 0
# window_logo_path none
# window_logo_position bottom-right
# window_logo_scale 1.0
#
# Font settings
# font_family DejaVuSansM Nerd Font
 font_size 13.0
# bold_font yes
# bold_font_weight 700
# italic_font auto
# bold_italic_font auto

# Terminal settings
#shell /bin/zsh --login -c 'if command -v tmux >/dev/null 2>&1; then tmux attach || tmux; else exec zsh; fi'

# Disable confirmation on exit
confirm_os_window_close 0

# Other settings
dynamic_background_opacity yes
# hide_window_decorations yes
